# ModTheSpire2 Clean Restart Manager Goal Entry

Use this file as the entry prompt when the next Goal-mode run should pursue the clean launcher and restart-based mod manager direction.

## Goal Prompt

Continue ModTheSpire2 development by strictly following:

```text
C:\Users\HZDH\Desktop\tmp\Forimpro\GOAL_CLEAN_RESTART_MANAGER.md
```

Also maintain the progress log:

```text
C:\Users\HZDH\Desktop\tmp\Forimpro\GOAL_HOT_RELOAD_PROGRESS.md
```

The current product direction is:

- Keep ModTheSpire2 as a clean native launcher and in-game fast restart helper.
- Let players change enabled mods by closing the current game and reopening the launcher.
- Do not present loaded DLL/PCK content mods as Hot-Apply candidates.
- Treat BaseLib-style mod configuration as future reference only, not as the current main goal.
- Preserve the latest Hot-Apply build as a blueprint/reference, but do not expose broad whole-mod Hot-Apply as the default workflow.

Before implementation, read `GOAL_CLEAN_RESTART_MANAGER.md` completely and treat it as the authoritative detailed target.
